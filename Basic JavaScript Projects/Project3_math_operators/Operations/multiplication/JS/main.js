function multiplying() { //named function "multiplying"
    var mult = 3 * 3; //named variable "mult" and gave it a value of 3*3
    document.getElementById("math").innerHTML = "3*3 = " + mult; //result should be 3*3=9
}