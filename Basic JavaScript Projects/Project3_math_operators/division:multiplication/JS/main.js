function both() { //named function "both"
    var multidiv = (3*3) + 6 / 1 - 3;
    document.getElementById("math").innerHTML = "3 multiplied by 3, plus 6, divided by 1 subtracted by 3 equals " + multidiv; //result should be 12
}